Since github cannot have csv files because they are too large, we have to manually put csvs under this folder.
By default, the databse we should start with is Base.csv.
For other csvs we can test them out once algorithims are finished.
